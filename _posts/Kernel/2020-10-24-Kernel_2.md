---
layout: post
title: "Kernel 분석 2"
categories: misc
---



|명령어|문법|설명|
|------|---|---|
|tst|TST{cond} Rn, Operand2|Rn의 값과 Operand2의 값을 비트 AND 연산을 수행한다. 결과에 따라서 N과 Z 플래그를 갱신한다.|
|Z flag|-|결과가 0이면 참이 된다|
|dc	civac,x0|-|C5.1.4|
|MRS|MRS R1, SCTLR|; writes the contents of the CP15 coprocessor ; register SCTLR into R1|
|adr_l|.macro	adr_l, dst, sym|adr은 +-4GB밖에 못봐서|
|STR|immediate offset|Store with immediate offset, pre-indexed immediate offset, or post-indexed immediate offset.|
|STP|STACK PUSH|Store Pair of Registers calculates an address from a base register value and an immediate offset, and stores two 32-bit words or two 64-bit doublewords to the calculated address, from two registers. For information about memory accesses, see   |
|ALIGH|he ALIGN directive aligns the current location to a specified boundary by padding with zeros or NOP instructions.|[ALIGH](https://www.keil.com/support/man/docs/armasm/armasm_dom1361290002364.htm)|
|xzr|register|0 대신에 xzr을 쓴다|
|SWAPPER_MM_MMUFLAGS|mmu flags|커널에서 사용하는 memory page table의 특성|
|[identity paging](https://wiki.osdev.org/Identity_Paging)||Identity Paging, Identity Mapped Paging and 1:1 Paging are terms often used on the forum to describe a design choice where a portion of virtual addresses are mapped to physical addresses that have the same value. This means that if paging is enabled with identity paging, 0xb8000 is 0xb8000, as long as that area is identity mapped.|
|.||- 링커 스크립트에서 .(dot)은 현재 위치를 나타내는 location counter입니다.|
|adr||PC의 상대 주소를 얻어오는 매크로
|KASLR||주소 공간 레이아웃 무작위|
|u64||unsigned bit 64|
|[ptrs_per_pgd](https://patchwork.kernel.org/project/linux-arm-kernel/cover/1553058574-18606-1-git-send-email-bhsharma@redhat.com/)|||
|compile time| |컴파일 타임(compile time)은 컴파일러에 의해 수행되는 동작(컴파일 타임 동작), 성공적으로 컴파일되기 위해서 소스 코드가 충족해야 하는 프로그래밍 언어 요구사항(컴파일 타임 요구사항), 또는 컴파일 동안 추론될 수 있는 프로그램의 속성을 나타낸다. |
|runtime||프로그램이 수행되면서 일어나는 상태|
|bl||지정하는 주소로 PC이동|
|dmb sy||dmb는 메모리 배리어, 만약 별 다른 말이 없다면 sy 옵션 영향 범위가 전체 시스템. 즉 데이터 접근에 대한 순서 보장을 하겠다는 뜻|
|lr||링크 레지스터, 반환 주소 표시|
|bne||0이 아닌 경우 분기|
|[base{, #imm}]|Base plus offset|addressing means that the address is the value in the 64-bit base register plus an offset|
|[base, #imm]!|Pre-indexed|addressing means that the address is the sum of the value in the 64-bit base register and an offset, and the address is then writtenback to the base register.|
|[base], #imm |Post-indexed|addressing means that the address is the value in the 64-bit base register, and the sum of the  address and the offset is then written back to the base register|
|PGDIR_SHIFT|ARM64_HW_PGTABLE_LEVEL_SHIFT(4 - CONFIG_PGTABLE_LEVELS|determines the size a top-level page table entry can map (depending on the configuration, this level can be 0, 1 or 2).|
|PTRS_PER_PGD|(1 << (VA_BITS - PGDIR_SHIFT))| |
|||![PTE](http://jake.dothome.co.kr/wp-content/uploads/2015/10/pgd-3.png)|
|str_l|.macro  str_l, src, sym, tmp| 현재 주소에서 +-4G 이내 범위에 위치한 심볼 @sym |주소에 32비트 또는 64비트 @dst 레지스터 값을 기록한다.
|sections|.pushsection section-name .pushsection section-name,"flags","type" 
.pushsection section-name = "flags","type"|The assembler maintains a section stack, which is defined by the .pushsection and .popsection directives. These directives push and pop previously-defined sections to and from the section stack. The assembler may limit the depth of a section stack, but it must allow at least ten levels. The .pushsection directive pushes the current section onto the stack and switches to the section specified in the directive. The .pushsection directive, like the .section directive, has one of the following formats:|


![PTE](http://jake.dothome.co.kr/wp-content/uploads/2015/10/pgd-3.png)|
