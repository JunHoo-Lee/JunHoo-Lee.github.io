---
layout: post
title: "Binary Search Tree Iterator"
categories: leetcode
---

![ㅇㅇ](/images/문제.png)

복잡해 보이는데, 결국 inorder traversal로 list를 init하고 그거에 맞춰서 뱉어내라는 소리이다.

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class BSTIterator:

    def __init__(self, root: TreeNode):
        self.curheight = 0
        self.nodes = []
        if root != None:
            self.append(root)
        #print(self.nodes)
        self.nextval = 0
        
        

    def next(self) -> int:
        
        return self.nodes.pop(0)
        

    def hasNext(self) -> bool:
        
        isnext = len(self.nodes) > 0
        #print(isnext)
        return isnext
        
    def append(self,node) -> bool:
        if node.left:
            self.append(node.left)
        self.nodes.append(node.val)
        if node.right:
            self.append(node.right)
        
        


# Your BSTIterator object will be instantiated and called as such:
# obj = BSTIterator(root)
# param_1 = obj.next()
# param_2 = obj.hasNext()
```

엄청 바보처럼 짰는데, 문제 조건에선 이걸 여러번 실행할수 있게 하라는 말이 없길래 걍 pop로 읽어들이게 만들었다..ㅋㅋ 그래서 memory usage면에서 상위 99.88%를 차지하였다.

![memory](/images/2020-12-10-12-10-21.png)