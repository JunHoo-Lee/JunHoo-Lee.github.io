---
layout: post
title: "Basic Calculator II"
categories: leetcode
---

![2](/images/2020-11-27-11-28-50.png)

다음과 같은 상황이다. 풀다가 화딱지나서 근본없이 풀었다.

나는 이 문제를 총 세가지의 문제로 보았는데,

1. string 객체를 operand, operator로 분리하기
2. operator 중 * , / 를 우선적으로 처리하기
3. 이후 더하기를 처리하기

1,2 를 묶어서 하려다가 화딱지나서 그냥 re lib를 import해서 풀었다.

```python
import re

class Solution(object):
    def calculate(self, s):
        """
        :type s: str
        :rtype: int
        """
        # whitespce를 없애고
        sentence = s.replace(" ","")
        # 연산기호들로 split한 결과를 operand로 저장하고
        operand = re.split("\*|\+|\-|\/|\\s+",sentence)
        b = re.split("\\d+",sentence)
        operator = []
        # 이다음에 whitespace로 분리했더니 이상한 whitespace가 남아서 

        for op in b:
        # For문으로 풀어주고..
            if op == "*" or op == "/" or op == "-" or op == "+":
                operator.append(op)
        newoperand = []
        newoperator = []
        expected_length = 1
        # 첫번째 for 문에서 곱하기를 제거한 후
        if(len(operator)>0):
            for opidx in range(len(operator)):
                op = operator[opidx]
                expected_length += 1
                if op == "*":
                    expected_length -= 1
                    if expected_length <= len(newoperand):
                        num = int(newoperand[-1]) * int(operand[opidx + 1])    
                        newoperand[-1] = num
                    else: 
                        num = int(operand[opidx]) * int(operand[opidx + 1])
                        newoperand.append(num)
                elif op == "/":
                    expected_length -= 1
                    if expected_length <= len(newoperand):
                        num = newoperand[-1] / int(operand[opidx + 1])    
                        newoperand[-1] = num
                    else: 
                        num = int(operand[opidx]) / int(operand[opidx + 1])
                        newoperand.append(num)
                else:
                    newoperator.append(operator[opidx])
                    if opidx > 0 and (operator[opidx - 1] == "*" or operator[opidx -1] == "/"):
                        pass
                    else :
                        newoperand.append(int(operand[opidx]))

                    if opidx == len(operator) -1:
                        newoperand.append(int(operand[opidx+1]))
        else: return int(operand[0])

        # 두번째에서 더하기를 제거한다.
        if len(newoperand) != 0:
            ret = newoperand[0]
            for i in range(len(newoperator)):
                op = newoperator[i]
                if op == "+":
                    ret += newoperand[i+1]
                else:
                    ret-= newoperand[i+1]
        return ret
        '''
        for i in range(len(s)):
            char = s[i]
            if char == "*" or char == "/" or char == "+" or char =="-":
                operand.append(char)
                s[i] = (" ")
                
        '''
                
        
        
        
        #return ret
```

결과는 다음과 같이 나왔다.

너무 근본이 없어서 다른 사람 코드도 소개한다

```python
class Solution:
    def calculate(self, s: str) -> int:
        #re를 쓰지 않고 space를 없앴다. space로 split 한 후 합쳤네
        s = "".join(s.split())
        
        total = 0
        lastNum = 0
        currNum = 0
        lastop = '+'
        
        for i in range(len(s)):
            # 아 이런식으로 계속해서 10 곱해가면서 연속하는 수를 읽었네
            if s[i].isnumeric():
                currNum = currNum*10 + int(s[i])
            
            if not s[i].isnumeric() or i == len(s)-1:
                
                if lastop =='+':
                    total+=lastNum
                    lastNum=currNum
                
                elif lastop == '-':
                    total+=lastNum
                    lastNum = -currNum
                
                elif lastop=='*':
                    lastNum*=currNum
                
                elif lastop=='/':
                    if lastNum<0:
                        lastNum = math.ceil(lastNum/currNum)
                    else:
                        lastNum//=currNum
                
                lastop = s[i]
                currNum = 0
                
        total+=lastNum
        return total
```
... 이거 돌아가긴 하나?

